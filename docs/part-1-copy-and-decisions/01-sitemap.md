# 01 · Sitemap

9 pages. Every one a real route at its own address, reachable from a real navigation menu. **No anchor links standing in for pages** — that is the core structural change.

| Page | Route | Job |
|---|---|---|
| Home | `/` | Say who he is, who he helps, where — plain language above the fold. Replace "Efficient Frontier" headline. One clear action: start a conversation. **Signpost to About, not the pitch itself.** |
| **About** ⭐ MOST IMPORTANT | `/about` | The page a referred prospect actually reads. Real photo. Real background — in industry since 2012. Why he does this work. What being a fiduciary means in practice. First person throughout. Full name + Arizona location unmistakable — must win the name search. |
| Approach | `/approach` | Tzur Magen 3-Layer Process explained so a non-specialist follows it. Diagram the 21→7 narrowing. Process, never results. |
| Who I work with | `/who-i-work-with` | Lets the wrong reader leave and the right one recognise themselves. Situation-based — already built wealth, near/in retirement, wants active risk mgmt. **No dollar figures or minimums.** |
| Fees | `/fees` | Published wording reproduced exactly (see `02-preserved-substance.md`). Plain-language structure explanation. **No worked example.** |
| Custody | `/custody` | Where client money is actually held and why that matters. Existing transparency content is a strong start. Quietly answers "could this person steal from me?" |
| How we start (NEW) | `/how-we-start` | Client's request: "roadmap of how you onboard as a client." Client still has to define steps — build structure, flag content gap. |
| Contact | `/contact` | Real page. Booking + email + phone + city. Currently Calendly; scheduling tooling is being changed — **check with Arham before wiring anything.** |
| Insights | `/insights` | Build the shell empty. Newsletter + white paper land here later. **No filler blog posts** — filler here is worse than emptiness. Build so a gated/ungated PDF drops in cleanly later. Ask before building any email-capture gate. |

## Nav

A real `<nav>` element listing 8 items (Insights can appear in nav or footer — decide with copy). No anchor-scroll fakery.
